{
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"api,web\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix kanban-api\" \"npm run dev --prefix kanban-frontend\""
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "gsap": "^3.14.2"
  }
}
